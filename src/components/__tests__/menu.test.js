import { render, fireEvent } from '@testing-library/react'

import Menu from "../menu/menu"

describe('Menu', () => {
  it('renders a focusable button that activates the menu', () => {
    
  });

});